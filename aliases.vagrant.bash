# Some common aliases, modify to personal preference.
alias h=history                                  # vagrant
alias hg="history | grep "                        # vagrant
alias ht="history | tail " # vagrant

alias dc=cd # vagrant

alias gitb='git branch' # vagrant
alias gits='git status' # vagrant
alias gitss='git status --short' # vagrant

alias cdhdr='cd /var/www/harris/docroot' # vagrant
alias cdh='cd /var/www/harris' # vagrant
